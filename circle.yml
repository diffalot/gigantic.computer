machine:
  timezone: America/New_York
  node:
    version: 6

dependencies:
  override:
    - npm install

test:
  override:
    - npm test

compile:
  override:
    - npm run build

deployment:
  production:
    branch: master
    commands:
      - npm run deploy:ci
